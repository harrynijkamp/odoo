<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_res_partner_filter" model="ir.ui.view">
            <field name="name">res.partner.filter</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_res_partner_filter" />
            <field name="arch" type="xml">
                <xpath expr="//search" position="inside">
                    <filter string="Voiceover" name="voiceover_filter" domain="[('category_id', 'ilike', 'Voiceover')]" help="Filter contacts by tags" position="-10">
                        <field name="category_id" widget="many2many_tags" placeholder="Select tags..."/>
                    </filter>
                </xpath>
            </field>
        </record>
    </data>
</odoo>